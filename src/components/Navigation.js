import React from "react";
import NavMain from './NavMain';

const Navigation = () => {
    return (
        <nav className='navigation'>
            <NavMain />
        </nav>
    );
}

export default Navigation;